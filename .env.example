# Synq environment configuration.
# Copy this file to ".env" and fill in values as features are added.

# AI service (Google Gemini).
GEMINI_API_KEY=

# Future OAuth integrations (not used yet).
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# SQLite database path (relative to project root).
DATABASE_URL=sqlite:///./synq.db
