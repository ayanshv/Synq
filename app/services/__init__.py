"""Business logic services for Synq.

Services sit between the UI and the database. Each service owns one area of
the product, keeping UI code focused on display and model code focused on
data shape. Importing this package is optional; modules are usually imported
directly where needed (for example `from app.services.update_service import ...`).
"""
